apiVersion: 1

contactPoints:
  - orgId: 1
    name: default-webhook
    receivers:
      - uid: default-webhook
        type: webhook
        settings:
          url: "http://localhost:9999/webhook"
          httpMethod: POST
        disableResolveMessage: false
